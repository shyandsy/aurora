# ============================================
# Aurora Sample Project - Environment Configuration
# ============================================
# Copy this file to .env and update the values according to your environment

# ============================================
# Server Configuration (必需)
# ============================================
# 服务器监听地址
HOST=0.0.0.0

# 服务器监听端口
PORT=8080

# 服务名称（用于 API 路径前缀，如 /api/{SERVICE_NAME}/v1）
SERVICE_NAME=sample

# 服务版本
SERVICE_VERSION=1.0.0

# 运行级别: local, stage, production
# - local: 开发环境，使用 debug 模式
# - stage: 预发布环境
# - production: 生产环境，使用 release 模式
RUN_LEVEL=local

# HTTP 服务器超时配置（可选，有默认值）
READ_TIMEOUT=30s
WRITE_TIMEOUT=30s
IDLE_TIMEOUT=60s
SHUTDOWN_TIMEOUT=5s

# ============================================
# Database Configuration (必需)
# ============================================
# 数据库驱动: mysql 或 sqlite
DB_DRIVER=mysql

# 数据库连接字符串 (DSN)
# MySQL 格式: user:password@tcp(host:port)/dbname?charset=utf8mb4&parseTime=True&loc=Local
# SQLite 格式: file:database.db?cache=shared&mode=rwc
DB_DSN=root:123456@tcp(localhost:3306)/sample_db?charset=utf8mb4&parseTime=True&loc=Local

# 数据库连接池配置
DB_MAX_IDLE_CONNS=10
DB_MAX_OPEN_CONNS=100

# ============================================
# Redis Configuration (必需 - JWT 功能需要)
# ============================================
# Redis 服务器地址 (格式: host:port)
REDIS_ADDR=localhost:6379

# Redis 密码（必需，如果 Redis 没有密码，可以设置为 "none"）
REDIS_PASSWORD=123456

# Redis 数据库编号 (必须 >= 0)
REDIS_DB=0

# ============================================
# JWT Configuration (必需)
# ============================================
# JWT 密钥（必需，生产环境必须修改，不能使用默认值）
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# JWT 过期时间（必需，格式: 数字+单位，如 24h, 720h）
# 示例: 24h (24小时), 720h (30天), 168h (7天)
JWT_EXPIRE_TIME=24h

# JWT 签发者标识
JWT_ISSUER=sample-service

# ============================================
# I18N Configuration (可选，有默认值)
# ============================================
# 默认语言
I18N_DEFAULT_LANG=en

# 支持的语言列表（逗号分隔）
I18N_SUPPORTED_LANGS=en,zh-CN,zh-TW

# 国际化文件目录（相对于项目根目录）
I18N_LOCALE_DIR=locales

# 是否加载嵌入的国际化文件（可选）
# I18N_LOAD_EMBEDDED=true

# ============================================
# Mail Configuration (可选，如果不需要邮件功能可以不配置)
# ============================================
# 邮件服务器地址
# MAIL_SMTP_HOST=smtp.gmail.com

# 邮件服务器端口
# MAIL_SMTP_PORT=587

# 邮件发送者邮箱
# MAIL_FROM_EMAIL=noreply@example.com

# 邮件发送者名称
# MAIL_FROM_NAME=Sample Service

# 邮件服务器用户名
# MAIL_SMTP_USER=your_email@gmail.com

# 邮件服务器密码（或应用专用密码）
# MAIL_SMTP_PASSWORD=your_app_password
